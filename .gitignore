_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.DS_Store
**/.DS_Store
**/**/.DS_Store
# Gemfile.lock is tracked on purpose: the Actions build runs bundler in
# deployment (frozen) mode, which requires a committed lockfile that
# matches the Gemfile and includes the x86_64-linux platform.
assets/img/.DS_Store

# Tumblr auto-publisher
.github/scripts/node_modules
.github/scripts/.env
.github/scripts/README.md
.github/scripts/SETUP-CHECKLIST.md
